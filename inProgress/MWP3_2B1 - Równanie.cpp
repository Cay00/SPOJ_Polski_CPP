#include <iostream>
#include <string>

using namespace std;

int main()
{
    int testy;
    cin >> testy;

    string linia;

    while (testy--)
    {
        int liczby = 0, niewiadoma = 0;
        cin >> linia;

        for (int i = 0; i < linia.length(); i++)
        {
            if()
        }
    }

    return 0;
}